{
  "name": "@remvst/game-navigation",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build:core": "cd core && npm run build",
    "build:html": "cd html && npm run build",
    "build:react": "cd react && npm run build",
    "build:pixi": "cd pixi && npm run build",
    "build": "npm run build:core && npm run build:html && npm run build:react && npm run build:pixi",
    "postinstall": "scripts/fullinstall.sh"
  },
  "author": "Rémi Vansteelandt",
  "license": "UNLICENSED",
  "dependencies": {},
  "devDependencies": {}
}
